import type { ScrapedWebsiteData } from "./types"

/**
 * Scrape a business's existing website using Firecrawl.
 * Implementation will be completed in Phase 3.
 */
export async function scrapeWebsite(
  url: string,
): Promise<ScrapedWebsiteData | null> {
  // Phase 3: Implement Firecrawl integration
  // 1. Call Firecrawl API to scrape the given URL
  // 2. Extract headings, paragraphs, menu items, about text, images
  // 3. Map response to ScrapedWebsiteData

  throw new Error("Not implemented — will be completed in Phase 3")
}
