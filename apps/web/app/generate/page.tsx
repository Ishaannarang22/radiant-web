import { GenerationFlow } from "@/components/generate/generation-flow"

export const metadata = {
  title: "Generate a Website — Radiant Web",
  description: "Enter a business name and location to generate a complete, modern website in 60 seconds.",
}

export default function GeneratePage() {
  return <GenerationFlow />
}
